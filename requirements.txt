# FastAPI is the HTTP boundary for deterministic Pricing Intelligence.
fastapi>=0.115,<1.0

# Used by the API test client and installed for local backend testing.
httpx>=0.27,<1.0

# Test runner for the backend suite.
pytest>=9.0,<10.0

