# Data

`data/sample/` is reserved for small, non-sensitive sample files used during
local development or tests. Do not commit personal artisan information,
production uploads, credentials, or unapproved datasets. Local uploads and
datasets are ignored by Git.

