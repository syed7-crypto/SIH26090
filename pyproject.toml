[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sih26090"
version = "0.1.0"
description = "Foundation for SIH26090 artisan market linkage and smart cataloging."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]

